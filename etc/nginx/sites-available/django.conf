server {
    listen 8001;
    server_name 18.185.30.117;
    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }
    location /static/ {
        root /home/ubuntu/webapp;
    }
    location / {
        include uwsgi_params;
        uwsgi_pass unix:/run/uwsgi/webapp.sock;
    }
}